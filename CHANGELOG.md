# Changelog

เลขเวอร์ชันแบบ **MAJOR.MINOR.PATCH** — แก้ที่ไฟล์ `VERSION` ที่เดียว
- **PATCH** (0.2.0 → 0.2.1) แก้บั๊ก ไม่มีของใหม่
- **MINOR** (0.2.x → 0.3.0) เพิ่มความสามารถใหม่
- **MAJOR** (0.x → 1.0.0) พร้อมใช้จริงเต็มตัว / เปลี่ยนใหญ่จนของเก่าใช้ไม่ได้

เลข build (`CFBundleVersion`) ขึ้นเองตามจำนวน commit ไม่ต้องแก้

## 0.6.3 — 2026-09-22
- เปลี่ยน: ข้อความอัปเดตกลับไปโชว์แค่เวอร์ชันล่าสุดเวอร์ชันเดียว (ไม่รวมของเก่าเหมือนก่อนหน้านี้)
  ต่อท้ายด้วยลิงก์ "ดูการเปลี่ยนแปลงทั้งหมด" ไปหน้า CHANGELOG เต็มบน GitHub แทน
  เครื่องที่ตามทันอยู่แล้วไม่ต้องเห็นของเก่าซ้ำ ส่วนเครื่องที่ห่างหายไปนานคลิกดูเองได้

## 0.6.2 — 2026-09-22
- แก้: กด ⌥Space ทันทีหลังเคาะ space ไปแล้ว (เช่นคำเพิ่งโดนย้อนกลับเป็นตัวดิบ) ไม่มีอะไรเกิดขึ้นเลย
  สาเหตุ: พอกด space ตัวแปรที่เก็บคำไว้ถูกล้างทันที ⌥Space เลยไม่มีอะไรให้แก้
  ตอนนี้แอปจำคำที่เพิ่งจบไปแบบไม่ถูกแก้ไว้ให้ กด ⌥Space ย้อนหลังได้จนกว่าจะพิมพ์ตัวใหม่ต่อ

## 0.6.1 — 2026-09-22
- แก้: ประโยคที่แปลงถูกและสลับแป้นไปแล้วจริงระหว่างพิมพ์ ถูกย้อนกลับเป็นตัวดิบทั้งประโยค
  แค่เพราะมีคำสแลง/คำนอกพจนานุกรมปนมาคำเดียว (เช่น "อยุ" พิมพ์ตกจาก "อยู่")
  → ด่านตรวจซ้ำตอนจบคำ เปลี่ยนไปเช็คแค่ "สะกดเป็นไทยอ่านได้ไหม" แทนการเช็คทุกคำในพจนานุกรม
- แก้: ตัวตรวจการสะกดไทย (isWellFormedThai) เดิมไม่จำกัดว่าพยัญชนะ 1 ตัวแบกสระ/วรรณยุกต์ได้กี่ตัว
  ทำให้ขยะแบบ "ฟี้ี้ี" (สระซ้อนกันไม่รู้จบ) หลุดผ่านมาได้ — ตอนนี้จำกัดพยัญชนะละไม่เกิน 1 สระบน/ล่าง
  + 1 วรรณยุกต์ + 1 สระตามหลัง ตามหลักพยางค์ไทยจริง

## 0.6.0 — 2026-09-22
- ใหม่: **ซิงค์คำศัพท์จากอินเทอร์เน็ตอัตโนมัติ** — เพิ่มคำสแลง/ชื่อเฉพาะใหม่ๆ ได้โดยไม่ต้องออกเวอร์ชันแอปใหม่
  แอปเช็คทุก 6 ชั่วโมง (เงียบๆ ไม่มีป๊อปอัป) เจอคำใหม่ก็เพิ่มให้เอง คำที่ผู้ใช้เติมเองไม่มีวันถูกทับ
  ปุ่ม "อัปเดตคลังคำ" ใน Settings ตอนนี้ดึงจากอินเทอร์เน็ตด้วย ไม่ใช่แค่จากตัวแอป
- เครื่องมือใหม่ (ฝั่ง Nice): `./add_word.sh th|en <คำ>` เพิ่มคำแล้วขึ้น GitHub ให้ทุกเครื่องดึงเอง

## 0.5.3 — 2026-09-22
- แก้: เพิ่มคำ "อัพเดธ" (สะกดไม่เป็นทางการ) เข้าคลังคำ — เดิมไม่มีในพจนานุกรม (มีแต่ "อัปเดต"/"อัพเดต")
  ทำให้ประโยคที่มีคำนี้ปน เช่น "เดี๋ยวลองอัพเดธดูก่อน" ตัดคำไม่ออกและถูกย้อนกลับตอนกด space

## 0.5.2 — 2026-09-22
- แก้: `release.sh` ตอนนี้รวม changelog ทุกเวอร์ชันที่ข้ามมาไว้ในข้อความอัปเดตเดียว
  (เดิมโชว์แค่เวอร์ชันล่าสุด เครื่องที่ห่างหายไปหลายรอบจะพลาดเห็นว่ามีอะไรเปลี่ยนไปบ้าง)

## 0.5.1 — 2026-09-22
- เปลี่ยน: เช็คอัปเดตทุก 4 ชั่วโมง (เดิมวันละครั้ง)

## 0.5.0 — 2026-09-22
- ใหม่: **แจ้งเตือนตอนมีอัปเดตใหม่** ผ่าน macOS Notification Center — สำหรับเครื่องที่เปิดแอปทิ้งไว้
  เจออัปเดตจากการเช็คพื้นหลัง (ไม่ใช่ตอนกด Check for Updates… เอง) จะเด้งแจ้งเตือน คลิกแล้วเปิดหน้าต่างอัปเดตให้เลย
  (ทำแบบนี้เพราะ LangFix ไม่มีไอคอนใน Dock ปกติจะไม่มีอะไรเตือนเลยถ้าไม่ใช้ระบบแจ้งเตือน)
- ครั้งแรกที่เปิดแอปจะมีกล่องถามอนุญาตแจ้งเตือน ต้องกด Allow ถึงจะได้ป้ายนี้ (ไม่กดก็ยังเช็คอัปเดตเองจากเมนูได้ปกติ)

## 0.4.2 — 2026-09-22
- แก้บั๊กสำคัญ: ประโยคที่พิมพ์ถูกต้องระหว่างพิมพ์ กลับกลายเป็นขยะตอนกด space
  (เช่น "ตอนนี้ที่มีปัญหาคืออะไร" พิมพ์ถูกอยู่แล้ว แต่กด space แล้วกลายเป็น "9vนนี้...")
  สาเหตุ: พอแก้คำได้แล้วแอปสลับภาษาแป้นพิมพ์ระบบให้อัตโนมัติ ตัวที่พิมพ์ต่อจากนั้น
  ระบบเลยส่งมาเป็น "ภาษาเป้าหมาย" ที่ถูกแล้ว แต่แอปยังเก็บเป็นข้อมูลดิบแบบเดิม
  พอตรวจซ้ำตอนจบคำ ข้อมูลกลายเป็นข้อมูลผสมตัดคำไม่ออก เลยย้อนคำที่ถูกอยู่แล้วกลับเป็นขยะ

## 0.4.1 — 2026-09-21
- ทดสอบระบบอัปเดตอัตโนมัติครั้งแรก (ไม่มีการเปลี่ยนแปลงการทำงาน)

## 0.4.0 — 2026-09-21
- ใหม่: **อัปเดตอัตโนมัติ** — แอปเช็ควันละครั้ง เจอเวอร์ชันใหม่จะถามก่อนติดตั้ง
  หรือกด Settings → Check for Updates… ได้ทันที (ใช้ Sparkle 2.10)
- ใหม่: หน้า Settings แสดงเลขเวอร์ชันและ build

## 0.3.0 — 2026-09-21
- เปลี่ยน: **เลิกเดา** — แปลงเฉพาะเมื่อผลลัพธ์เป็นคำจริงในคลังคำ/รายชื่อเท่านั้น
  (เดิมเดาจาก "เรียงถูกหลักไทย" ทำให้ได้คำไม่มีความหมาย เช่น /com → ฝแนท)
- ใหม่: กด ⌥Space แปลงคำไหนเอง แอปจะ**จำคำนั้นไว้** ครั้งหน้าแปลงให้เอง (กดซ้ำทันที = ยกเลิกการจำ)
- ใหม่: คำที่แปลงไปตั้งแต่กลางคำ ถูกตรวจซ้ำตอนจบคำ ถ้าผลสุดท้ายอ่านไม่ได้ → ย้อนกลับเป็นที่พิมพ์
- แก้: ตัวตรวจการสะกดไทยไม่รู้จัก เ-าะ (เกาะ เคราะห์) และ ๆ
- ผลวัด: คำอังกฤษโดนแปลงผิด 8 → 2 จาก 3,000 คำ

## 0.2.1 — 2026-09-21
- แก้: คำสั่งขึ้นต้นด้วย `/` (เช่น `/compact` `/help`) และ path (`/Users`) โดนแปลงเป็นไทย
  ส่วนคำไทยขึ้นต้น ฝ ที่มีในคลังคำ (ฝาก ฝน ฝัน) ยังแปลงได้ตามปกติ

## 0.2.0 — 2026-09-21
- ใหม่: แพ็กเป็น DMG พร้อมไอคอน หน้าต่างติดตั้ง และไฟล์ `อ่านก่อน.txt`
- ใหม่: ติ๊กสิทธิ์ Accessibility แล้วเริ่มทำงานเอง ไม่ต้องเปิดแอปใหม่
- ใหม่: แสดงเลขเวอร์ชันในหน้าต่างแอป
- ใหม่ (สำหรับนักพัฒนา): `--why` ส่องเหตุผลทีละตัวอักษร, `--bench` วัดผลทั้งคลังคำ
- แก้: พิมพ์ประโยคยาวไม่เคาะ space แล้วไม่แปล ถ้าตัวอักษรแรกๆ บังเอิญเป็นต้นคำอังกฤษได้
- แก้: เปิดหน้าต่างครั้งแรกสวิตช์ขึ้นปิดหมด ต้องเปิดครั้งที่สองถึงถูก
- แก้: ป้าย "No need to press space" → "Fixes as soon as it's sure" ให้ตรงกับที่ทำจริง

## 0.1.0 — 2026-09-20
- เวอร์ชันแรก: แก้คำพิมพ์ผิดแป้นไทย↔อังกฤษอัตโนมัติ แก้ระหว่างพิมพ์ สลับแป้นให้ Esc/⌥Space
